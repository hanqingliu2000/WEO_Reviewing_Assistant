# Types Module

## Owns

Shared TypeScript types for review items, issue report entries, drafts, evidence, sessions, and output.

## Related Slices

- All frontend slices.

## Rules To Preserve

- Use stable object names from planning docs.
- Do not introduce persistent `skip` state.
- Keep types aligned with `docs/development/mock_data_spec.md` until real data contract exists.

## Current Files

- `review.ts` defines the shared frontend types used by mock data and future components.
